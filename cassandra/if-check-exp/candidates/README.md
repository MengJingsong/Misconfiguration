# Candidates

Working list of if-check candidates surfaced by the CodeQL queries in
`codeql-queries/cassandra/`, pending manual triage against the three rules in
[../README.md](../README.md) before being written up as a full case file
under one of the sibling module folders (e.g. `../memtable/`, `../net/`).
